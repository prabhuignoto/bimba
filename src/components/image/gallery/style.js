export default ({ color }) => ({
  gallery: {
    background: '#000',
    width: '100%',
  },
  galleryWrapper: {
    alignItems: 'center',
    display: 'flex',
    flexDirection: 'row',
    flexWrap: 'wrap',
    justifyContent: 'center',
  },
  searchInProgress: {
  },
});
