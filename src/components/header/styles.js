export default ({ color }) => ({
  appHeader: {
    alignItems: 'center',
    background: color.backgroundAccent,
    display: 'flex',
    flexDirection: 'row',
    minHeight: '70px',
    maxHeight: '90px',
    flexWrap: 'wrap',
  },
});
